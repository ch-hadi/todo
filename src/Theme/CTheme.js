import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  typography: {
    color: 'red !important',
    dispaly: 'none',
    fontFamily: 'Railway, sans-serif', // Use your custom font name here
  },
});

export default theme;
